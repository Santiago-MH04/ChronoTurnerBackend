package org.springboot.riwi.chronoturner.backend.controllers.genericMethods;

public interface Remove<Entity> {
    //Atributos de Remove
    //Constructores de Remove
    //Asignadores de atributos de Remove (setters)
    //Lectores de atributos de Remove (getters)
        //Métodos de Remove
    public void removeEntity(Entity entity);
}
