package org.springboot.riwi.chronoturner.backend.services.methodServices;

public interface SaveService<Entity, EntityRequestDTO>{
    //Atributos de SaveService
    //Constructores de SaveService
    //Asignadores de atributos de SaveService (setters)
    //Lectores de atributos de SaveService (getters)
        //Métodos de SaveService
    public Entity save(EntityRequestDTO entity);
}
